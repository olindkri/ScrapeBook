export const A4_WIDTH_MM = 210;
export const A4_HEIGHT_MM = 297;

// Default scale: pixels per mm for on-screen display
export const DEFAULT_SCALE = 3;

// Default image size when first added (mm)
export const DEFAULT_IMAGE_SIZE_MM = 50;
